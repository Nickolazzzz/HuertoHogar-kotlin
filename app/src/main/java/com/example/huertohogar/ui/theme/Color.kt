package com.example.huertohogar.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de Colores de HuertoHogar (Verde y Tierra)

// ------------------------------------
// COLORES PARA TEMA CLARO (LIGHT MODE)
// ------------------------------------

// Verde Principal para botones, barras y elementos de interacción
val PrimaryGreen = Color(0xFF388E3C) // Un verde oscuro, fuerte y orgánico
val OnPrimary = Color.White // Texto sobre el color principal

// Verde Claro/Acento para componentes secundarios o estados activos
val SecondaryGreen = Color(0xFF66BB6A) // Un verde medio/claro, más fresco
val OnSecondary = Color.Black // Texto sobre el color secundario

// Color para el fondo/Superficies de la aplicación
val BackgroundBeige = Color(0xFFFFFBE6) // Un beige muy claro, casi blanco cremoso (color tierra/natural)
val SurfaceLight = Color(0xFFFFFFFF) // Blanco puro para tarjetas y superficies
val OnSurfaceLight = Color(0xFF1C1B1F) // Texto principal oscuro

// ------------------------------------
// COLORES PARA TEMA OSCURO (DARK MODE)
// ------------------------------------

// Verde principal en Dark Mode (debe ser más claro para contrastar con el fondo oscuro)
val DarkPrimary = Color(0xFFA5D6A7)

// Fondo muy oscuro, con un toque verdoso
val DarkBackground = Color(0xFF0D1213)
// Superficies/Tarjetas oscuras
val DarkSurface = Color(0xFF1D2627)
// Texto principal blanco
val DarkOnSurface = Color.White